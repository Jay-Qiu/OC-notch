# OC-Notch — Local signing configuration
# Copy this file to local.mk and fill in your values.
# local.mk is gitignored and MUST NOT be committed.
#
# To find your values:
#   Team ID:     Apple Developer Portal → Membership → Team ID
#   Apple ID:    Your Apple Developer account email
#   App password: appleid.apple.com → Sign-In and Security → App-Specific Passwords

TEAM_ID         := YOUR_TEAM_ID
DEVELOPER_NAME  := Your Name
KEYCHAIN_PROFILE := OC-Notch-Notarize

# Store notarization credentials in Keychain (one-time setup):
#   xcrun notarytool store-credentials "$(KEYCHAIN_PROFILE)" \
#     --apple-id "your@email.com" \
#     --team-id "$(TEAM_ID)" \
#     --password "YOUR_APP_SPECIFIC_PASSWORD"

# ─── Sparkle Auto-Update ─────────────────────────────────────────
# One-time setup:
#   1. make setup-sparkle              (downloads Sparkle CLI tools)
#   2. tmp/sparkle-tools/bin/generate_keys  (generates EdDSA keypair)
#      → Private key: stored in your macOS Keychain — NEVER export or commit it
#      → Public key:  printed to stdout — paste it into Info.plist SUPublicEDKey
#
# Override Sparkle version or tools path if needed:
# SPARKLE_VERSION := 2.9.1
# SPARKLE_BIN     := /custom/path/to/sparkle/bin
